export enum Screen {
    Discover = 'discover',
    Following = 'following',
    Profile = 'profile',
}

export enum AuthScreenType {
    Welcome = 'welcome',
    Login = 'login',
    SignUp = 'signup',
    ProfileSetup = 'profile_setup',
}